% function that computes the relative translation of a revolute X joint, which, from parent frame, rotates first along X axis by \alpha
%   input: q = [\alpha] where n is the quaternion

function xlt = revolute_X_translation(~)

    xlt = zeros(3,1);

end