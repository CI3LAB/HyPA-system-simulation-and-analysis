% function that computes the relative translation of rotation of a revolute Z joint, which, from parent frame, rotates first along Z axis by \gamma
%   input: q = [\gamma] where n is the quaternion

function xlt = revolute_Z_translation(~)

    xlt = zeros(3,1);

end