% function that computes the relative translation of rotation of a revolute Y joint, which, from parent frame, rotates first along Y axis by \beta
%   input: q = [\beta] where n is the quaternion

function xlt = revolute_Y_translation(~)

    xlt = zeros(3,1);

end