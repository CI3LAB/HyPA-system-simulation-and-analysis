% function that computes the relative translation of a linear x joint
%   input: q = [x] where x is the position coordinates in the PARENT FRAME

function xlt = linear_X_translation(q)

    xlt = [q(1);0;0];

end