% function that computes the relative translation of a spherical joint
%   input: q = [alpha; beta; gamma] where alpha, beta and gamma are the Euler angles along X, Y and Z axes, respectively

function xlt = spherical_eulerXYZ_translation(~)

    xlt = zeros(3,1);

end