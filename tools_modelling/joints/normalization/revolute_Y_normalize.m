% function that normalizes the quaternion q, if applicable
%   input: q = [beta] where beta is the Euler angle along Y axis

function q_normalized = revolute_Y_normalize(q)
    % nothing special
    q_normalized = q;
end