% function that normalizes the quaternion q, if applicable
%   input: q = [alpha; gamma] where alpha and beta are the Euler angles along X and Z axes, respectively

function q_normalized = universal_XZ_normalize(q)
    % nothing special
    q_normalized = q;
end