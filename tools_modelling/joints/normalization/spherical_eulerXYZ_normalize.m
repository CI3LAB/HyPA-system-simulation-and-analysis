% function that normalizes the quaternion q, if applicable
%   input: q = [alpha; beta; gamma] where alpha, beta and gamma are the Euler angles along X, Y and Z axes, respectively

function q_normalized = spherical_eulerXYZ_normalize(q)
    % nothing special
    q_normalized = q;
end