% function that normalizes the quaternion q, if applicable
%   input: q = [gamma] where gamma is the Euler angle along Z axis

function q_normalized = revolute_Z_normalize(q)
    % nothing special
    q_normalized = q;
end