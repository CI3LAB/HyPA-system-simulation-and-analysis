% function that normalizes the quaternion q, if applicable
%   input: q = [alpha] where alpha is the Euler angle along X axis

function q_normalized = revolute_X_normalize(q)
    % nothing special
    q_normalized = q;
end