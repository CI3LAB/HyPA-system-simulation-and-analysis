% function that normalizes the quaternion q, if applicable
%   input: q = [alpha; beta] where alpha and beta are the Euler angles along X, Y and Z axes, respectively

function q_normalized = universal_XY_normalize(q)
    % nothing special
    q_normalized = q;
end